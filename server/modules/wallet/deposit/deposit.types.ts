// @ts-nocheck
// RECOVERED: this source file was missing from git history (never committed) while
// production kept running off a stale compiled dist/ via Docker build cache.
// Reconstructed verbatim from the last known-good compiled output on 2026-09-11.
// TODO: remove @ts-nocheck once someone re-adds proper types for this file.
// TODO: these were type-only exports erased from the compiled dist/ this file was
// reconstructed from — placeholder `any` shapes just restore buildability; someone
// should tighten them to real discriminated unions matching deposit.service.ts's usage.
export type DepositGasEstimateInput = any;
export type SubmitDepositOutcome = any;
