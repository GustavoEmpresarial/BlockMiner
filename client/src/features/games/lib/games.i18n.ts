import { featureT, type FeatureTFunction } from '../../../shared/utils/featureT';

export type TranslateFn = FeatureTFunction;

/** Games hub / arenas / 2048 — active i18next locale. */
export const t = featureT;
